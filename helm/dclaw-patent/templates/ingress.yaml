apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "dclaw-patent.name" . }}
  labels:
    app.kubernetes.io/name: {{ include "dclaw-patent.name" . }}
spec:
  rules:
    - host: dclaw-patent.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "dclaw-patent.name" . }}
                port:
                  number: {{ .Values.frontend.service.port }}
